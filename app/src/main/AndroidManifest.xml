<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab13_reloaded">

    <application
        android:name=".data.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.StudentListActivity"
            android:label="Lista de Estudiantes" />
        <activity
            android:name=".view.BaseActivity"
            android:label=" " />
        <activity
            android:name=".view.LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.AddStudentActivity"
            android:label="Mantenimiento de Estudiantes" />
        <activity
            android:name=".view.CourseListActivity"
            android:label="Lista de Cursos" />
        <activity
            android:name=".view.AddCourseActivity"
            android:label="Mantenimineto de Cursos" />
        <activity
            android:name=".view.SelectCourseActivity"
            android:label="Seleccione los cursos" />
    </application>

</manifest>